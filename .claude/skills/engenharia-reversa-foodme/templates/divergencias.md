# Divergências — código vs. spec/contrato aprovado

> Cada entrada apresenta as duas leituras lado a lado, sem veredito. A
> decisão é sempre do dono do produto, registrada depois na spec
> correspondente (como suposição confirmada, pergunta respondida, ou
> clarificação).

## DIV-001 — <resumo de uma frase do conflito>

- **O que o código faz**:
  - `<arquivo>:<linha>`
    ```
    <trecho literal>
    ```
- **O que a spec aprovada diz**:
  - `<.spec/features/<feature>/spec.md#AC-xxx>`
    > <citação literal>
- **Por que são incompatíveis**: <explicação neutra, sem apontar qual está "certo">
- **Impacto se não resolvido**: <o que pode dar errado em produção/negócio>
- **Status**: aberta | levada para <spec/feature> em <data> | resolvida em <data> (<resumo da decisão e quem decidiu>)

---

## DIV-002 — ...
